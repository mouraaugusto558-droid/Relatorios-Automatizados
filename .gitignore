node_modules/
dist/
release/
releases/

# dados e sessão do WhatsApp nunca devem ir para o git
# (mantém a estrutura de pastas via .gitkeep, ignora todo o resto do conteúdo)
storage/**
!storage/**/
!storage/**/.gitkeep

.env
*.local

npm-debug.log*
.DS_Store
Thumbs.db

# cache do ESLint (gerado localmente, não precisa ir para o git)
.eslintcache

# dump do codigo-fonte gerado localmente, nao faz parte do projeto
MeuNovoProjeto-codigo-fonte.zip
